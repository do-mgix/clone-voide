services:
  db:
    image: postgres:16
    container_name: voide-db
    environment:
      POSTGRES_USER: voide_user
      POSTGRES_PASSWORD: voide_user
      POSTGRES_DB: voide
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: