services:
  - type: web
    name: shopstore-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build && npx prisma generate
    startCommand: npx prisma migrate deploy && npm run start
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
